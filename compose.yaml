services:
  trello-dashboard:
    build: .
    container_name: trello-dashboard-container
    ports:
      - "8001:8000"
    volumes:
      - ./data:/app/data
      - ./app/static:/app/static
    restart: unless-stopped
